/**
 * Amin MOHAMED
 * Version 1.0
 */
import { Outlet } from 'react-router-dom';

/**
 * outlet component to render views of the application
 *
 * @returns {JSX.Element} a router outlet
 */
const RouterOutlet = () => {
  return <Outlet />;
};

export default RouterOutlet;
