import { Gallery } from '@/components';
import { MainLayout } from '@/layouts';

const HomePage = () => {
  return (
    <MainLayout>
      <Gallery />
    </MainLayout>
  );
};

export default HomePage;
